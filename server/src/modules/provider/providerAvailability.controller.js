const pool = require("../../config/db");
const service = require("./providerAvailability.service");
const schedule = async (req,res)=>{try{res.json({success:true,schedule:await service.getSchedule(req.user.id)});}catch(error){res.status(400).json({success:false,message:error.message});}};
const weekly = async (req,res)=>{try{res.json({success:true,schedule:await service.saveWeeklySchedule(req.user.id,req.body.slots)});}catch(error){res.status(400).json({success:false,message:error.message});}};
const blockDate = async (req,res)=>{try{if(!/^\d{4}-\d{2}-\d{2}$/.test(String(req.body.unavailable_date||"")))throw new Error("A valid unavailable_date is required.");res.status(201).json({success:true,blocked_date:await service.addBlockedDate(req.user.id,req.body.unavailable_date,req.body.reason)});}catch(error){res.status(400).json({success:false,message:error.message});}};
const unblockDate = async(req,res)=>{try{await service.removeBlockedDate(req.user.id,req.params.id);res.json({success:true,message:"Date unblocked successfully."});}catch(error){res.status(400).json({success:false,message:error.message});}};
const availability = async(req,res)=>{try{const providerId=Number(req.params.providerId),serviceId=Number(req.query.service_id),date=String(req.query.date||"");if(!Number.isInteger(providerId)||providerId<=0)throw new Error("Invalid provider ID.");if(!Number.isInteger(serviceId)||serviceId<=0)throw new Error("A valid service_id is required.");if(!/^\d{4}-\d{2}-\d{2}$/.test(date))throw new Error("A valid date query is required.");const provider=await pool.query("SELECT id,business_name FROM providers WHERE id=$1 AND is_active=TRUE",[providerId]);if(!provider.rows[0])return res.status(404).json({success:false,message:"Provider not found or inactive."});const selectedService=await pool.query(`SELECT s.id,COALESCE(ps.duration_minutes,s.duration_minutes,60) AS duration_minutes FROM services s JOIN provider_services ps ON ps.service_id=s.id AND ps.provider_id=$2 AND ps.is_active=TRUE WHERE s.id=$1`,[serviceId,providerId]);if(!selectedService.rows[0])return res.status(404).json({success:false,message:"This provider does not offer the selected service."});const scheduleResult=await pool.query(`SELECT is_available,start_time,end_time FROM provider_availability WHERE provider_id=$1 AND day_of_week=EXTRACT(DOW FROM $2::date)::smallint`,[providerId,date]);const blocked=await pool.query("SELECT 1 FROM provider_unavailable_dates WHERE provider_id=$1 AND unavailable_date=$2",[providerId,date]);const bookings=await pool.query(`SELECT b.booking_time,COALESCE(ps.duration_minutes,s.duration_minutes,60) AS duration_minutes FROM bookings b JOIN services s ON s.id=b.service_id LEFT JOIN provider_services ps ON ps.provider_id=b.provider_id AND ps.service_id=b.service_id AND ps.is_active=TRUE WHERE b.provider_id=$1 AND b.booking_date=$2 AND b.status IN ('Pending','Accepted','In Progress')`,[providerId,date]);const scheduleRow=scheduleResult.rows[0]||null;const duration=Number(selectedService.rows[0].duration_minutes||60);const toMinutes=v=>{const [h,m]=String(v).slice(0,5).split(":").map(Number);return h*60+m;};const toTime=m=>`${String(Math.floor(m/60)).padStart(2,"0")}:${String(m%60).padStart(2,"0")}`;const bookedRanges=bookings.rows.map(r=>{const start=toMinutes(r.booking_time);return{start,end:start+Number(r.duration_minutes||60)};});const slots=[];if(!blocked.rows[0]&&scheduleRow?.is_available&&scheduleRow.start_time&&scheduleRow.end_time){const start=toMinutes(scheduleRow.start_time),end=toMinutes(scheduleRow.end_time);for(let minute=start;minute+duration<=end;minute+=30){if(!bookedRanges.some(range=>minute<range.end&&minute+duration>range.start))slots.push(toTime(minute));}}res.json({success:true,provider:provider.rows[0],service:{id:serviceId,duration_minutes:duration},date,blocked:Boolean(blocked.rows[0]),schedule:scheduleRow,booked_times:bookings.rows.map(r=>String(r.booking_time).slice(0,5)),available_slots:slots});}catch(error){res.status(400).json({success:false,message:error.message});}};
module.exports={schedule,weekly,blockDate,unblockDate,availability};